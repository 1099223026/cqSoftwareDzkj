- 完善k线图开盘价，收盘价，最高最低价等三条sql
	+ 在给数组foreach赋值时候，应该用取地址符合（&）
	+ 填充断层时间算法
	- 根据timeshare来获取当前时段（天/时/分）
	- 对数组进行循环操作for
		+ 时间戳减去timeshare，看i++的时间戳是否存在
		+ 否：增加新的元素，并将当前项的最高价/最低价/开盘价/收盘价装载在新的元素中，成交量为0
		+ 是：continue