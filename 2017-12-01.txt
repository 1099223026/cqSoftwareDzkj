1、对昨天接口的tcp请求的封装
    yii2 model 要插入数据库，只需要在注释上用 * @property string $id的形式就可以 insert();
    用insert将订单信息录入数据库后,调用\Yii::$app->db->getLastInsertId()方法获取最后一个id,php中允许连等号（a=b=3）;
2、开盘价计算功能实现->也就是第一笔成交价格，9:00到9:30为用户委托时间，第一笔交易成交时间应该提交2分钟左右。
    首先在库中模拟几条数据；
    生成今天的时间戳（从0点开始）作为where条件；
    生成SQL并降序排序。
   